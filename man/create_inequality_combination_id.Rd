% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_inequality_combination_id.R
\name{create_inequality_combination_id}
\alias{create_inequality_combination_id}
\title{Create Inequality Combination ID}
\usage{
create_inequality_combination_id(
  df,
  imd_code_col = "imd_code",
  ethnicity_code_col = "ethnicity_code",
  overall_code = 999L
)
}
\arguments{
\item{df}{A data frame containing IMD and ethnicity code columns.}

\item{imd_code_col}{Name of the column containing IMD codes.
Defaults to \code{"imd_code"}.}

\item{ethnicity_code_col}{Name of the column containing ethnicity codes.
Defaults to \code{"ethnicity_code"}.}

\item{overall_code}{Code used to represent an overall result with no
inequality split. Defaults to \code{999L}.}
}
\value{
The original data frame with an additional integer
\code{combination_id} column.
}
\description{
Creates a \code{combination_id} that identifies whether each row contains an
IMD split, an ethnicity split, both, or neither.
}
\details{
This can be used downstream to distinguish overall results from inequality
breakdowns when filtering, reporting, or visualising metric-engine output.

The combination IDs are:

\itemize{
\item \code{1} - IMD and ethnicity splits are both present
\item \code{2} - IMD split only
\item \code{3} - Ethnicity split only
\item \code{4} - Neither split is present; the row represents an overall result
}

The value defined by \code{overall_code} is treated as representing no split.
By default, this is \code{999}.

Missing or unexpected IMD or ethnicity values are assigned
\code{NA} for \code{combination_id}.
}
