% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{run_sql_file}
\alias{run_sql_file}
\title{Run a SQL script from a file}
\usage{
run_sql_file(conn, path)
}
\arguments{
\item{conn}{An active database connection, typically created using
\code{DBI::dbConnect()}.}

\item{path}{A character scalar specifying the path to the SQL file
to be executed.}
}
\value{
\code{NULL} invisibly when the SQL script runs successfully.
A success message is printed to the console.
}
\description{
Reads a SQL script from a file and executes it against an existing
database connection.
}
\details{
The SQL file is read line by line using \code{readLines()} and collapsed into
a single SQL string. The resulting SQL script is then executed using
\code{DBI::dbExecute()}.

If the SQL script is executed successfully, a confirmation message is
printed containing the name of the SQL file.

If an error occurs while reading or executing the SQL script, the error
is caught using \code{tryCatch()}. A descriptive error message is raised using
\code{stop()}, including the SQL filename and the original database or file
error message. Execution of the current pipeline is stopped.
}
\section{Dependencies}{

This function uses:

\itemize{
\item \code{readLines()} - reads the contents of the SQL file.
\item \code{basename()} - extracts the SQL filename from the supplied path.
\item \code{DBI::dbExecute()} - executes the SQL script against the database.
\item \code{tryCatch()} - handles errors encountered during execution.
}
}

