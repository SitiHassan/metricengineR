% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_pooled_data.R
\name{create_pooled_data}
\alias{create_pooled_data}
\title{Create Pooled Multi-Year Data}
\usage{
create_pooled_data(
  df,
  span_years = c(3L, 5L),
  pool_keys = c("indicator_id", "imd_code", "aggregation_id", "age_group_code",
    "sex_code", "ethnicity_code", "creation_date", "value_type_code", "source_code",
    "combination_id")
)
}
\arguments{
\item{df}{A data frame containing yearly records to be pooled. It must include
the pooling key columns, \code{start_date}, \code{numerator}, \code{denominator}, and
\code{period_type}.}

\item{span_years}{Integer vector giving the number of years to include in each
pooled period. Defaults to \code{c(3L, 5L)}.}

\item{pool_keys}{Character vector of column names used to group records before
pooling.}
}
\value{
A data frame containing pooled records with summed \code{numerator} and
\code{denominator} values and newly created \code{start_date}, \code{end_date}, and
\code{time_period_type} columns.
}
\description{
Creates pooled multi-year data from yearly Calendar or Financial year records.
Numerators and denominators are summed across rolling year windows such as
3-year or 5-year periods.
}
\details{
Only rows where \code{period_type} is \code{"Calendar"} or \code{"Financial"} are included.

Calendar pooled periods run from 1 January to 31 December. Financial year
pooled periods run from 1 April to 31 March.

Only complete pooling windows are returned. For example, a 3-year pooled
result is only created when all three years are present.
}
