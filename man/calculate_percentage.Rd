% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_percentage.R
\name{calculate_percentage}
\alias{calculate_percentage}
\title{Calculate percentages}
\usage{
calculate_percentage(
  df,
  value_type_code_col = "value_type_code",
  numerator_col = "numerator",
  denominator_col = "denominator",
  value_multiplier_col = "value_multiplier",
  confidence_intervals_required = FALSE,
  ci_method = "proportion"
)
}
\arguments{
\item{df}{A data frame containing the values to calculate.}

\item{value_type_code_col}{Name of the column containing value type codes.
Defaults to \code{"value_type_code"}.}

\item{numerator_col}{Name of the numerator column.
Defaults to \code{"numerator"}.}

\item{denominator_col}{Name of the denominator column.
Defaults to \code{"denominator"}.}

\item{value_multiplier_col}{Name of the column containing the value multiplier.
Defaults to \code{"value_multiplier"}.}

\item{confidence_intervals_required}{Logical indicating whether confidence
intervals should be calculated. Defaults to \code{FALSE}.}

\item{ci_method}{Confidence interval method. Currently only \code{"proportion"}
is supported. Defaults to \code{"proportion"}.}
}
\value{
A data frame containing rows with value type code 2 and the calculated
columns \code{value}, \code{lowercl}, and \code{uppercl}.
}
\description{
Calculates percentage values for rows with value type code 2.
Confidence intervals can optionally be calculated using a proportion method.
}
\details{
Rows are eligible for calculation when numerator, denominator, and value
multiplier are present and the denominator is greater than zero.

When confidence intervals are not required, percentage values are calculated
as numerator divided by denominator multiplied by the value multiplier.
Confidence interval columns are returned as \code{NA}.

When confidence intervals are required, eligible rows must represent valid
proportions where the numerator is between zero and the denominator.
\code{PHEindicatormethods::phe_proportion()} is used to calculate the value and
95 percent confidence intervals.

Ineligible rows are retained and their calculated values remain \code{NA}.
}
