% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_output.R
\name{tidy_output}
\alias{tidy_output}
\title{Tidy metric calculation output}
\usage{
tidy_output(data)
}
\arguments{
\item{data}{A data frame containing metric calculation results.}
}
\value{
A data frame containing the standard metric engine output columns
in a consistent order. Missing standard columns are added and populated
with \code{NA}.
}
\description{
Standardises the structure of metric calculation outputs so that results
from different calculation functions can be combined using a consistent
set of columns and column names.
}
\details{
The function validates that \code{data} is a data frame and then standardises
alternative calculation output column names to the metric engine schema.

The following alternative column names are handled:

\itemize{
\item \code{value} is used to populate missing values in \code{indicator_value}.
\item \code{lowercl} is used to populate missing values in \code{lower_ci95}.
\item \code{uppercl} is used to populate missing values in \code{upper_ci95}.
}

If the standard column does not exist, it is created from the corresponding
alternative column where available.

If both the standard column and its alternative column are present,
existing non-missing values in the standard column are retained, while
missing values are populated from the corresponding alternative column.

Any standard output columns that are not present in the input data are
created and populated with \code{NA}.

The final output is restricted to the standard metric engine columns and
returned in the following order:

\itemize{
\item \code{indicator_id}
\item \code{start_date}
\item \code{end_date}
\item \code{numerator}
\item \code{denominator}
\item \code{indicator_value}
\item \code{lower_ci95}
\item \code{upper_ci95}
\item \code{imd_code}
\item \code{aggregation_id}
\item \code{age_group_code}
\item \code{sex_code}
\item \code{ethnicity_code}
\item \code{creation_date}
\item \code{value_type_code}
\item \code{source_code}
\item \code{time_period_type}
\item \code{combination_id}
}

Columns in the input data that are not part of the standard output schema
are removed from the returned data frame.
}
\section{External dependencies}{

This function uses:

\itemize{
\item \code{dplyr::select()} - selects and orders the standard output columns.
\item \code{dplyr::all_of()} - ensures that all standard output columns are
selected in the defined order.
}
}

