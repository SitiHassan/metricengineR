% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{replace_indicator_data_in_sql}
\alias{replace_indicator_data_in_sql}
\title{Replace indicator data in a SQL table}
\usage{
replace_indicator_data_in_sql(
  conn,
  database_name,
  schema_name,
  table_name,
  data,
  indicator_ids = "All",
  id_column = "indicator_id"
)
}
\arguments{
\item{conn}{An active database connection, typically created using
\code{DBI::dbConnect()}.}

\item{database_name}{A character scalar specifying the database containing the
target table.}

\item{schema_name}{A character scalar specifying the schema containing the target
table.}

\item{table_name}{A character scalar specifying the name of the SQL table into
which the data should be loaded.}

\item{data}{A data frame containing the records to append to the SQL table
after the existing records have been deleted.}

\item{indicator_ids}{A vector of numeric/character indicator IDs identifying which existing
records should be deleted before the new data are appended.

When \code{"All"} is supplied, all existing rows in the table are deleted
before the new data are appended. Defaults to \code{"All"}.}

\item{id_column}{A character scalar specifying the name of the column used
to identify indicators in the target SQL table. Defaults to
\code{"indicator_id"}.}
}
\value{
\code{NULL} invisibly when the operation completes successfully.
A message is printed indicating whether all rows or only selected
indicator rows were replaced.
}
\description{
Deletes existing records from a SQL table and appends new metric data using
an existing database connection. The function can either replace all rows
in the table or replace only rows associated with selected indicator IDs.
}
\details{
The supplied \code{indicator_ids} are first processed using
\code{normalise_indicator_ids()}.

The target table and indicator ID column are represented using
\code{DBI::Id()} and safely quoted using \code{DBI::dbQuoteIdentifier()}.

If specific indicator IDs are supplied, the function constructs a
\code{DELETE} statement containing a \code{WHERE} clause for those IDs. Indicator
values are safely quoted using \code{DBI::dbQuoteLiteral()}.

If \code{"All"} is supplied and \code{normalise_indicator_ids()} returns \code{NULL},
no \code{WHERE} clause is added and all existing rows in the target table are
deleted.

The deletion and subsequent append operation are performed inside a single
database transaction using \code{DBI::dbWithTransaction()}. This ensures that if
the insert fails after the deletion has started, the transaction is rolled
back rather than leaving the target table partially updated.

New data are appended using \code{DBI::dbWriteTable(..., append = TRUE)}.

Errors occurring during table identification, deletion, or insertion are
caught using \code{tryCatch()}. If an error occurs, a descriptive error is
raised using \code{stop()} and execution of the current pipeline is stopped.
}
\section{Internal dependencies}{

This function depends on:

\itemize{
\item \code{normalise_indicator_ids()} - standardises the supplied indicator
ID input
}
}

\section{External dependencies}{

This function uses:

\itemize{
\item \code{DBI::Id()} - creates the database table identifier.
\item \code{DBI::dbQuoteIdentifier()} - safely quotes table and column
identifiers.
\item \code{DBI::dbQuoteLiteral()} - safely quotes indicator ID values.
\item \code{DBI::dbWithTransaction()} - performs deletion and insertion within
a single database transaction.
\item \code{DBI::dbExecute()} - executes the SQL \code{DELETE} statement.
\item \code{DBI::dbWriteTable()} - appends the supplied data to the target SQL
table.
\item \code{tryCatch()} - handles errors during the database operation.
}
}

