% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_crude_rate.R
\name{calculate_crude_rate}
\alias{calculate_crude_rate}
\title{Calculate crude rates}
\usage{
calculate_crude_rate(
  df,
  value_type_code_col = "value_type_code",
  numerator_col = "numerator",
  denominator_col = "denominator",
  value_multiplier_col = "value_multiplier",
  confidence_intervals_required = FALSE
)
}
\arguments{
\item{df}{A data frame containing the values to calculate.}

\item{value_type_code_col}{Name of the column containing value type codes.
Defaults to \code{"value_type_code"}.}

\item{numerator_col}{Name of the numerator column.
Defaults to \code{"numerator"}.}

\item{denominator_col}{Name of the denominator column.
Defaults to \code{"denominator"}.}

\item{value_multiplier_col}{Name of the column containing the value multiplier.
Defaults to \code{"value_multiplier"}.}

\item{confidence_intervals_required}{Logical indicating whether confidence
intervals should be calculated. Defaults to \code{FALSE}.}
}
\value{
A data frame containing crude rate rows with calculated \code{value},
\code{lowercl}, and \code{uppercl} columns.
}
\description{
Calculates crude rates for rows identified as crude rate value types.
Confidence intervals can optionally be calculated using
\code{PHEindicatormethods::phe_rate()}.
}
\details{
Rows are eligible for calculation when the numerator, denominator, and value
multiplier are present and the denominator is greater than zero.

Crude rates are calculated as numerator divided by denominator multiplied
by the value multiplier. Numerators cannot be negative.

When confidence intervals are required, \code{PHEindicatormethods::phe_rate()}
is used to calculate the crude rate and 95 percent confidence intervals.
Eligible rows must use the same value multiplier.

Ineligible rows are retained with calculated values set to \code{NA}.
}
