% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_dasr.R
\name{calc_dasr}
\alias{calc_dasr}
\title{Calculate directly age-standardised rates}
\usage{
calc_dasr(
  df,
  age_metadata,
  value_type_code_col = "value_type_code",
  numerator_col = "numerator",
  denominator_col = "denominator",
  value_multiplier_col = "value_multiplier",
  confidence_intervals_required = TRUE
)
}
\arguments{
\item{df}{A data frame containing the values to calculate.}

\item{age_metadata}{A data frame containing \code{indicator_id} and
\code{single_age_code}.}

\item{value_type_code_col}{Name of the column containing value type codes.
Defaults to \code{"value_type_code"}.}

\item{numerator_col}{Name of the numerator column.
Defaults to \code{"numerator"}.}

\item{denominator_col}{Name of the denominator column.
Defaults to \code{"denominator"}.}

\item{value_multiplier_col}{Name of the column containing the value multiplier.
Defaults to \code{"value_multiplier"}.}

\item{confidence_intervals_required}{Logical indicating whether confidence
intervals should be retained. Defaults to \code{TRUE}.}
}
\value{
A data frame containing DASR results with calculated \code{value},
\code{lowercl}, \code{uppercl}, and final \code{age_group_code}.
}
\description{
Calculates directly age-standardised rates for rows with value type code 4
using \code{PHEindicatormethods::calculate_dsr()}.
}
\details{
DASRs are calculated using the ESP 2013 standard population.
Calculations are performed separately for each value multiplier so that
\code{PHEindicatormethods::calculate_dsr()} receives a single multiplier value.

Each DASR calculation must contain one consistent value multiplier.

When confidence intervals are not required, \code{lowercl} and \code{uppercl} are
returned as \code{NA}.
}
