% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_complete_periods.R
\name{filter_complete_data}
\alias{filter_complete_data}
\title{Exclude Incomplete Reporting Periods}
\usage{
filter_complete_data(
  df,
  period_type_col = "period_type",
  start_date_col = "start_date",
  end_date_col = "end_date",
  time_period_col = NULL,
  indicator_col = NULL,
  current_date = Sys.Date(),
  enforce_duration = TRUE,
  tolerance_days = 5L
)
}
\arguments{
\item{df}{A data frame containing reporting period data.}

\item{period_type_col}{Name of the column containing the reporting period
type, such as \code{"Calendar"} or \code{"Financial"}.
Defaults to \code{"period_type"}.}

\item{start_date_col}{Name of the column containing period start dates.
Defaults to \code{"start_date"}.}

\item{end_date_col}{Name of the column containing period end dates.
Defaults to \code{"end_date"}.}

\item{time_period_col}{Optional name of the column containing the time period
type, such as \code{"Monthly"}, \code{"Quarterly"}, or \code{"1 year"}.
Defaults to \code{NULL}.}

\item{indicator_col}{Optional name of the indicator identifier column.
Defaults to \code{NULL}.}

\item{current_date}{Date used to determine the latest completed reporting
cycle. Defaults to \code{Sys.Date()}.}

\item{enforce_duration}{Logical indicating whether records should also be
checked against the typical duration of the reporting period.
Defaults to \code{TRUE}.}

\item{tolerance_days}{Number of days a record duration may differ from the
typical duration and still be considered valid. Defaults to \code{5L}.}
}
\value{
The original data frame with incomplete or invalid reporting periods
removed.
}
\description{
Removes rows belonging to reporting periods that are not yet complete.
}
\details{
The function uses completed historical records to identify the typical
reporting period end date and, optionally, the typical duration of each
reporting cycle.

The typical reporting cycle end date is identified from completed historical
records using the most common month and day of the end date.

For monthly data, the latest completed period is the end of the previous
calendar month.

When \code{enforce_duration = TRUE}, records whose duration differs from the
typical duration by more than \code{tolerance_days} are excluded.

A message is printed showing how many rows were excluded.
}
\examples{
example_data <- data.frame(
  period_type = c(
    "Calendar",
    "Calendar",
    "Calendar"
  ),
  start_date = as.Date(
    c(
      "2023-01-01",
      "2024-01-01",
      "2025-01-01"
    )
  ),
  end_date = as.Date(
    c(
      "2023-12-31",
      "2024-12-31",
      "2025-12-31"
    )
  )
)

filter_complete_data(
  df = example_data,
  period_type_col = "period_type",
  start_date_col = "start_date",
  end_date_col = "end_date",
  current_date = as.Date("2025-06-01")
)
}
