% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{get_indicators_from_sql}
\alias{get_indicators_from_sql}
\title{Extract indicators from a SQL table}
\usage{
get_indicators_from_sql(
  conn,
  schema_name,
  table_name,
  database_name = NULL,
  indicator_ids = NULL
)
}
\arguments{
\item{conn}{An active database connection, typically created using
\code{DBI::dbConnect()}.}

\item{schema_name}{A character scalar specifying the database schema
containing the table.}

\item{table_name}{A character scalar specifying the name of the SQL table
from which indicator data should be extracted.}

\item{database_name}{An optional character scalar specifying the database
containing the table. If \code{NULL}, the database associated with the
existing connection is used. Defaults to \code{NULL}.}

\item{indicator_ids}{An optional numeric or character vector containing
valid indicator IDs to extract. Accepted values include:

\itemize{
\item \code{NULL} to extract all indicators.
\item \code{"All"} to extract all indicators.
\item A numeric vector such as \code{c(1, 2, 3)}.
\item A character vector such as \code{c("1", "2", "3")}.
\item A single comma-separated character string, such as
\code{"1, 2, 3"}.
}

\code{"All"} must be supplied on its own. Defaults to \code{NULL}.}
}
\value{
A data frame containing the records returned by the SQL query.
On successful execution, messages are printed confirming that the
extraction completed and reporting the total number of rows extracted.
}
\description{
Extracts indicator data from a SQL database table using an existing
database connection. The function can return all indicators or filter
the data to a specified set of indicator IDs.
}
\details{
The function first validates \code{indicator_ids} to ensure that the supplied
values are either numeric or character.

A SQL table identifier is then created using \code{DBI::Id()}. When
\code{database_name} is supplied, the identifier contains the database, schema,
and table. When \code{database_name} is \code{NULL}, only the schema and table are
used.

The table identifier is safely quoted using \code{DBI::dbQuoteIdentifier()}.

If \code{indicator_ids} is \code{NULL} or equal to \code{"All"}, all rows from the
specified table are returned. Otherwise, each indicator ID is safely
quoted using \code{DBI::dbQuoteLiteral()} and added to a
\verb{WHERE indicator_id IN (...)} clause.

The resulting SQL query is executed using \code{DBI::dbGetQuery()}.

Errors occurring while constructing or executing the SQL query are caught
using \code{tryCatch()}. If an error occurs, a descriptive error is raised using
\code{stop()} and execution of the current pipeline is stopped.
}
\section{Dependencies}{

This function uses:

\itemize{
\item \code{DBI::Id()} - creates a database table identifier.
\item \code{DBI::dbQuoteIdentifier()} - safely quotes the database table
identifier.
\item \code{DBI::dbQuoteLiteral()} - safely quotes indicator ID values.
\item \code{DBI::dbGetQuery()} - executes the SQL query and returns the result.
\item \code{tryCatch()} - handles errors during SQL query construction and
execution.
}
}

\section{Internal dependencies}{

This function depends on:

\itemize{
\item \code{normalise_indicator_ids()} - normalises the supplied indicator IDs
and identifies requests to extract all indicators.
}
}

