% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transformations.R
\name{clean_data_types}
\alias{clean_data_types}
\title{Clean and standardise metric data types}
\usage{
clean_data_types(data)
}
\arguments{
\item{data}{A data frame containing metric data to be standardised.
The data frame must contain the following columns:

\itemize{
\item \code{indicator_id}
\item \code{start_date}
\item \code{end_date}
\item \code{numerator}
\item \code{denominator}
\item \code{indicator_value}
\item \code{lower_ci95}
\item \code{upper_ci95}
\item \code{imd_code}
\item \code{aggregation_id}
\item \code{age_group_code}
\item \code{sex_code}
\item \code{ethnicity_code}
\item \code{creation_date}
\item \code{value_type_code}
\item \code{source_code}
\item \code{time_period_type}
\item \code{combination_id}
}}
}
\value{
The input data frame with the specified columns converted to their
standard metric engine data types.
}
\description{
Converts columns within a metric data frame to the standard data types
expected by the metric engine.
}
\details{
The function applies a consistent data type schema to metric data before
further processing or combining outputs from different calculation
functions.

The following conversions are applied:

\itemize{
\item Identifier and coded fields are converted to integer.
\item \code{start_date} and \code{end_date} are converted to \code{Date}.
\item Numerators, denominators, indicator values, and confidence intervals
are converted to numeric.
\item \code{creation_date} is converted to \code{POSIXct}.
\item \code{time_period_type} is converted to character.
}

Before conversion, the function checks that \code{data} is a data frame and that
all required columns are present. If required columns are missing, execution
stops and the missing column names are reported.

Values that cannot be converted to the required data type may be converted
to \code{NA} and may generate a warning from the relevant base R conversion
function.
}
\section{External dependencies}{

This function uses:

\itemize{
\item \code{dplyr::mutate()} - applies the required data type conversions.
}
}

